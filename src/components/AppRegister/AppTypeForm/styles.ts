import { createStyles, makeStyles, Theme } from '@material-ui/core';

// material ui styles for app type form component
export const appTypeFormStyles = makeStyles((theme: Theme) =>
    createStyles({
        layout: {
            marginLeft: theme.spacing(3),
        },
    }),
);
